const { test, expect, request } = require('@playwright/test');
test("Fixtures demo", async ({ page }) => {
//Login application and verify order is created
// await page.goto('url');
//This authenticatedPage has to have login information
authenticatedPage.goto(""); 
});
